include header
#main
    form(action="/", method="POST")
        textarea(name="markdown", id="markdown") #{markdown}
        br
        button(type="submit") Format!
p(id = 'display')

include sidebar
include footer
